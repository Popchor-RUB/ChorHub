# Frontend environment variables — development mode.
# Run ./setup-secrets.sh to generate frontend/.env.development (symlinked as .env.frontend.dev).
# Vite loads this file automatically when running in development mode (npm run dev).
VITE_API_URL=http://localhost:5173/api
VITE_BASE_PATH=/
